# Runtime deps for the jigsaw generator (jigsaw.py + modules):
shapely>=2.0                  # polygon Boolean ops, containment, edge merging
opencv-python-headless>=4.8  # letter contour tracing (cv2.findContours)
Pillow>=10.0                 # letter rasterization, image loading, mockup rendering
numpy>=1.24                  # array math for geometry + raster
scipy>=1.10                  # Delaunay triangulation for --vertex-grid tiling
pyyaml>=6.0                  # material profile loading (profiles/laser_materials.yaml)
# Test runner:
pytest>=7.0

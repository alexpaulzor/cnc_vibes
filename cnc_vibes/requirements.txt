pyyaml>=6.0
pytest>=7.0
pyserial>=3.5
Pillow>=10.0
# For scripts/find_cnc.py (mDNS discovery of Grbl_ESP32 controllers):
zeroconf>=0.130
# For scripts/cam.py engrave_text (rasterize text → findContours), and
# for lessons/laser/03_jigsaw geometry tracing:
opencv-python-headless>=4.8
# For scripts/cam.py (shapely geometry input):
shapely>=2.0
# For scripts/openscad_loader.py (parse OpenSCAD's SVG export into shapely):
svgelements>=1.9
# For scripts/cam_cli.py (interactive `cnc.py cam` wizard):
prompt_toolkit>=3.0
# Optional for snapshot --camera mode (uses cv2, already pulled in above)
